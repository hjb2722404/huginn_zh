<div class='container'>
  <div class='row'>
    <div class='col-md-8 col-md-offset-2'>
      <div class='well'>

        <h2>编辑 <%= resource_name.to_s.humanize %></h2>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: 'form-horizontal' }) do |f| %>
          <%= devise_error_messages! %>

          <div class="form-group">
            <%= f.label :email,"邮箱", class: 'col-md-4 control-label' %>
            <div class="col-md-6">
              <%= f.email_field :email, autofocus: true, class: 'form-control' %>
            </div>
          </div>

          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>目前正在等待确认: <%= resource.unconfirmed_email %></div>
          <% end %>

          <div class="form-group">
            <%= f.label :username,"用户名", class: 'col-md-4 control-label' %>
            <div class="col-md-6">
              <%= f.text_field :username, class: 'form-control' %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :current_password,"旧密码", class: 'col-md-4 control-label' %>
            <div class="col-md-6">
              <%= f.password_field :current_password, class: 'form-control' %>
              <span class='help-inline'>我们需要您当前的密码来确认您的更改.</span>
            </div>
          </div>

          <div class="form-group">
            <div class="col-md-offset-4 col-md-10">
              <%= f.submit "更新", class: "btn btn-primary" %>
            </div>
          </div>
        <% end %>

        <h3>更改密码</h3>
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: 'form-horizontal' }) do |f| %>
          <%= devise_error_messages! %>
          <div class="form-group">
            <%= f.label :current_password,"旧密码", class: 'col-md-4 control-label' %>
            <div class="col-md-6">
              <%= f.password_field :current_password, class: 'form-control' %>
              <span class='help-inline'>我们需要您当前的密码来确认您的更改.</span>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :password,"新密码", class: 'col-md-4 control-label' %>
            <div class="col-md-6">
              <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
            </div>
          </div>

          <div class="form-group">
            <%= f.label :password_confirmation,"确认密码", class: 'col-md-4 control-label' %>
            <div class="col-md-6">
              <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
            </div>
          </div>

          <div class="form-group">
            <div class="col-md-offset-4 col-md-10">
              <%= f.submit "更新", class: "btn btn-primary" %>
            </div>
          </div>

        <% end %>

        <hr>

        <h3>取消我的帐户</h3>

        <p>不快乐? <%= link_to "取消我的帐户", registration_path(resource_name), data: { confirm: "您确定吗?" }, method: :delete %></p>

        <%= link_to "返回", :back %>
      </div>
    </div>
  </div>
</div>
